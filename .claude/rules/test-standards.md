---
paths:
  - "Assets/Tests/**/*.cs"
---

# Test Standards

- Structure every test as: Arrange / Act / Assert
- One logical assertion per test (multiple Assert calls checking one behavior are fine)
- Use descriptive test names — what is being tested and what is expected
- qa agent defines additional standards as needed
